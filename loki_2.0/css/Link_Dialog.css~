#types_pane {
	width:170px;
	float:left;
}
/* .tabset div#pane_wrapper {
	background-image:url(../images/1px_grey.gif);
	background-repeat:repeat-y;
	background-position:20% 0;
} */

.tabset div#pane_wrapper:after {
    content: "."; 
    display: block; 
    height: 0; 
    clear: both; 
    visibility: hidden;
}

.tabset div.tabs_chunk, .tabset div.tabpanel_chunk.selected {display: inline-block;}

/* Hides from IE-mac \*/
* html .tabset div.tabs_chunk, * html .tabset div.tabpanel_chunk.selected {height: 1%;}
.tabset div.tabs_chunk, .tabset div.tabpanel_chunk.selected {display: block;}
/* End hide from IE-mac */

#types_pane {
	height:9em;
	overflow:auto;
	/* background-color:#f0f3f7; */
	background-color:#eee;
	border-right:1px solid #999;
}
#types_pane ul {
	padding:0;
	list-style:none;
	margin:0;
}
#links_pane {
	/* float:left; */
	padding:.67em 0 0 1.25em;
	width:100%
	height:9em;
	overflow:auto;
}
#pane_wrapper {
	margin:1em 0 0 0;
	border:1px solid #999;
	background:#f3f3f3 url(../images/pane_wrapper_bg.gif) repeat-x left bottom;
	min-height:9em;
}
#types_pane a {
	display:block;
	color:black;
	text-decoration:none;
	padding:.25em .5em;
	padding-left:14px;
}
#types_pane li {
	/* border-bottom:1px solid #c6ccd7; */
	border-bottom:1px solid #ccc;
}
#types_pane li.selected {
	background:#797979 url(../images/type_selected_shadow.gif) repeat-x left bottom;
}
#types_pane li.selected a {
	cursor:default;
	margin-right:0;
	/* background:#667ba7 url(../images/type_selected_3.gif) no-repeat 4px center; */
	background:transparent url(../images/type_selected_5.gif) no-repeat 4px center;
	color:#fff;
	/* border-color:#667ba7; */
	border-color:#797979;
}
#types_pane a:hover {
	/* background-color:#cad1df; */
	background-color:#d1d1d1;
}

#links_pane label, #links_pane select, #links_pane input {
	display: block;
}
#links_pane select#links_select {
	margin-bottom:1em;
}

/* Hide links_pane unless selected */
/*
#links_pane>div {
	display:none;
}
#links_pane>div.selected, #links_pane>div.selected {
	display:block;
}
*/

.fieldset { /* XXX: need to make a fake elem or some such, not use this hack! */
	clear:both;
}
#other_options {
	display:none;
}
